[tool.poetry]
name = "checkio1"
version = "0.1.0"
description = "This project to convert a bluerred image with low 
resolution to a high resolution image to use it as a wallpaper to my project"
authors = ["Hussein Tarhini <husseintarhini168@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
